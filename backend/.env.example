# Server Settings
PORT=3000

# Database Settings
DB_USER=postgres
DB_HOST=localhost
DB_NAME=qimlik
DB_PASSWORD=your_password
DB_PORT=5432

# Gateway Security (Must match the Android App)
GATEWAY_API_KEY=key
